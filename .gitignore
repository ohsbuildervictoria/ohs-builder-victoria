node_modules
dist
dist-ssr
*.local
.DS_Store
.vercel
.env
.env.*
npm-debug.log*

.vercel

# Local working documents & scratch (not part of the app)
*.pdf
*.docx
*.pptx
SWMS/
# Root docs/ only — public/docs/ ships the Help Centre's media assets.
/docs/
pdf_pages/
pptx_slides/
zoomed/
ohs-builder-victoria/
debug.log
git_push*.bat
git_push*.ps1
git_push*.vbs
git_output.log
TIME_TRACKER.json

.temp/

# Local agent permissions — machine-specific, not part of the app
.claude/settings.local.json

# Live verification scripts — they sign in as real QA accounts, so they hold
# passwords and stay on this machine.
*.local.mjs
graphify-out/

# Education demo seed output (accounts + invite links) — local only
*.local.json
