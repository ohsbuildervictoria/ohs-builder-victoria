# Handover — 25–28 Aug 2026 (Daniel investigation → David's 26 Aug release → policy-page fix)

Owner: Saroj Sanjel (Nexxt Nest Group). Production: https://ohsbuildervictoria.com.au
(Cloudflare Pages auto-deploys from GitHub `main`; Supabase prod ref `bbbtqhypdjrmlrdabumm`,
staging `adoatvmvmttdvrdfpfve`; Resend for app email; Zoho for human mailboxes.)
Read alongside `ACCESS_RESET_AND_STAKEHOLDER_CLEANUP_2026-08-22.md` (everything up to 24 Aug).

---

## 1. 25 Aug — Daniel Romano "Verified → Pending" investigation (NO CHANGES MADE)

David reported Daniel Romano's documents flipped from Verified to Pending after the 24 Aug release.

**Conclusion: EXPECTED, not a regression.** The production record is worker **72 "Daniel Romana"**
(org 54, project 85, David's own email — the spelling difference is David's typo). His three
documents (white card / insurance / medical, docs 51–53, uploaded 22 Aug by David's account while
it held role `worker`) have `verified_at = null` because **nobody ever verified them** — the old UI
showed "Verified" purely because a file existed (`docExpiryStatus` pre-027; the 001 schema comment
even says "Missing (no file) -> Verified"). Migration 027 added the verification columns with no
backfill, correctly. The org-54 audit trail contains zero verify/approve events. Cohort audit:
Daniel's 3 docs were the ONLY current personal compliance documents on the whole platform — nobody
else affected. David simply needs to open each document and click ✓ Verify document (he has since
done exactly this for other stakeholders — see §5).

## 2. 25 Aug — Go-live commercial plan (PLAN ONLY, nothing built)

Artifact (private, share from its menu): https://claude.ai/code/artifact/031ae91b-2cd5-4418-aa22-05443b87d64f

Key content: current-state audit (what's live vs the billing gaps), branded-email plan (fix:
Supabase Auth SMTP → Resend per GO_LIVE_RUNBOOK "SMTP TODO"; DMARC still `p=none`), market-checked
pricing (**keep $89/$189 tiers** — HazardCo $219–299/mo +GST + $380/project, SiteConnect from
$333/mo; headline: "no per-project fees"), education licences ($3,900 / $7,900 per year + $39
student overage, invoiced), Stripe under Nexxt Nest Group (recommend a second Stripe account
branded OHS Builder Victoria), a 9–11 day build list, and 6 owner decisions (§10 of the artifact).
**Stripe environment was NOT inspected — the Stripe MCP connector needs one-time OAuth (`/mcp` →
claude.ai Stripe, or plugin OAuth link).** David independently priced the app at "$2,500–$3,000/yr
insurance… a no-brainer" in his 24 Aug email — validates the Professional tier.

## 3. 26 Aug — David's "updates OHSBuilderVictoria" email (Zoho inbox, 11:13 PM) — 4 items

Investigated then shipped as one release. Commit **`3e287ac`** (27 Aug), migration
**`029_returning_stakeholders.sql`** (staging-tested, then prod; ledger clean 001–029), deployed
bundle `index-CD1ZPl2h.js`, new endpoint `/api/send-policy-email`.

| Item | Root cause / request | Fix |
|---|---|---|
| Eric & Anitha couldn't join a new project | Proven from code + prod audit: (a) join page was signup-only — an existing email dead-ended; (b) `my_sites()`/`switch_my_site()` (024) were never wired into the worker UI; (c) personal documents hung off one membership. On 26 Aug no worker rows were even created for new project 97 — the students re-uploaded onto their old project-92 records. NOT session/cache. | 029: same-org same-email adds auto-link to the existing account (trigger); `person_compliance_documents` view — white card/insurance/medical follow the PERSON, induction/quiz/SWMS stay per site; person-wide supersede in `file_compliance_document`; additive RLS so a person reads all their own memberships. UI: "Sign in & add this site" mode on the join page; **My Sites** switcher in the worker portal; builder sees "Site added to their account" instead of a pointless invite. **Answer for David: NO deactivation needed, ever.** |
| Add Stakeholder / Add Company confusion | Two entry points on different tabs | "+ Add Stakeholder" and "+ Add Subcontractor" stacked top-right of Stakeholder Compliance, mutually exclusive; "Company" wording → "Subcontractor" |
| "What is Polices" + content | Wrong wording, missing plan types | "Policy & Management Plan Types" banner (his 8 types, worded as common practice not legal checklist) + his a–g document-category content |
| "Template" → Policy Email | Wanted policy distribution | Top-level **Policy Email** tab: one project → tick published policies → recipient preview → send. BCC-only, 7-day signed PDF links, server-composed recipients, `POLICY_EMAIL_SENT` audit row, never auto-records acknowledgement. Template library kept one click deep ("Start from a template" in Policy Register). |

**Production E2E proof (disposable QA org, fully deleted afterwards):** person joined Site One →
uploaded white card (Pending ✓ truthful-compliance intact) → builder added same email to Site Two →
auto-linked → switcher showed both sites → Site Two started its own 0/4 induction → white card
carried over → ONE verify click turned both site rows Verified → policy email "✓ Sent to 1
recipient" with correct scoping + audit row. QA org 68, its 2 projects, 2 memberships, policy,
2 auth accounts and storage files were all deleted; org 54 and the two real builder sign-ups
untouched. Tests: lint 0 errors, build clean, staging SQL acceptance, full prod browser E2E.

Same email thread also contained: **"Fwd: Wraping up gate 1"** — the 1906 Agency chasing David's
unpaid NextGen Gate-1 invoice and claiming IP until paid; Saroj deactivated
nextgenbuildconsultants.com. Business/legal call between Saroj and David — deliberately not touched.

## 4. 28 Aug — Policy page duplication fix

David (texting while testing): the Policy Register said the same seven topics twice — old
"Document Categories" chips + the new "Inside an OH&S Management Plan" card. His a–g list was meant
to REPLACE the categories content. Merged into one "Document Categories" card using his
descriptions; plan-types banner kept. Commit **`29495ac`**, live bundle `index-Bu4a-4lh.js`.
No data/dropdown values changed.

## 5. Education portal logins + usage check (28 Aug)

Working demo set = **Owner Test Institute (institution 2, is_demo)** — credentials in
`docs/EDUCATION_OWNER_TEST_ACCOUNTS.local.md` (git-ignored; admin/assessor/student, login at
/login with portal param). Sent to David by email. The older Internal Test Institute accounts
(institution 1) are deactivated.

**Usage as of Fri 28 Aug 01:00 Melbourne: nobody has used the education portal** — all three demo
accounts' last sign-in is Sat 22 Aug 19:07 (Saroj's own setup test); zero EDU audit events. What
David HAS done: Wed 26 ~22:22 verified Eric's three documents (real use of the verify flow!);
Wed 26 22:41–46 and Thu 27 09:45–47 worked the Policy Register (add/edit/delete policies — the
session where he found the duplication). Eric signed in Thu 16:26, Anitha Wed 20:20. Several new
accounts (his Academy students, by the look of it) were active Thu — not investigated (privacy).

## 6. Open items / next steps

1. **Stripe**: authorize the connector (`/mcp` → claude.ai Stripe), inspect the Nexxt Site Manager
   account, then decide separate-account-vs-shared (plan artifact §5/§10, decision #1).
2. **Owner decisions 1–6** in the go-live plan artifact (pricing GST basis, trial style, education
   pricing, David/Arlington agreement, GST + trading-name registration).
3. **Supabase Auth SMTP → Resend** still TODO (password resets come from Supabase's generic
   mailer) + staged DMARC tightening — Phase 0 of the plan, ~half a day.
4. Nudge David to test the education portal (logins sent, unused).
5. Consider rotating the Owner Test Institute passwords after David's testing (shared demo creds).
6. NextGen / 1906 Agency invoice-and-IP claim — Saroj + David business decision, outstanding.
7. Housekeeping: Saroj's own Chrome session (nexxtsitesolutions@gmail.com) was signed out during
   27 Aug testing — just sign back in. `C:\tmp\gettok.ps1` (used by old *.local.mjs diagnostics)
   is gone; `supabase db query --linked` / `supabase storage` are the better path now (CLI is
   authenticated; prod is the linked project — re-link staging only deliberately, and re-link prod
   after).

## 7. Commits this period (all on main, all deployed)

- `136f2b7` / `1b733f8` — records (24 Aug, prior session)
- `3e287ac` — David 26 Aug feedback: 029 returning stakeholders, Add pair, policy content, Policy Email
- `29495ac` — Policy page duplication fix (28 Aug)
